{
  "name": "mrfrac.ru",
  "version": "1.0.0",
  "description": "Personal web site",
  "author": "mrfrac <fracturer@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && parcel build src/index.html --out-dir build --public-url ./",
    "watch": "npm run clean && parcel src/index.html watch --out-dir build",
    "clean": "rm -rf build/*.{map,js,html,png,css}",
    "test": "echo 'None'"
  },
  "dependencies": {
    "game-of-life": "git+https://github.com/mrfrac/game-of-life.git"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.38.2"
  }
}
